id: policy.baseline
version: 1
purpose: A single requirement that the estate violates (NonCompliant).
requirements:
  - id: req.secret-scanning
    strategy:
      identifier: PredicateEvaluation
      version: 1
    predicate:
      equals:
        setting: secret_scanning
        value: enabled
