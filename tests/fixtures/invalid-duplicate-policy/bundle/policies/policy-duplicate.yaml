id: policy.baseline
version: 1
purpose: A second document reusing the same (id, version) — ambiguous.
requirements:
  - id: req.dependabot
    strategy:
      identifier: PredicateEvaluation
      version: 1
    predicate:
      equals:
        setting: dependabot
        value: enabled
